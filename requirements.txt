# Core dependencies for the SLAM pipeline and all evaluations.
# Versions are the ones the reported results were produced with.
opencv-python-headless>=4.8
numpy>=1.24
scipy>=1.10
PyYAML>=6.0
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7

# Optional -- only needed to revive src/cnn_loop_closure.py (see the note in
# that file). None of the reported results depend on it; the retrained
# classifier deliberately omits the CNN similarity feature.
# torch>=2.0
# torchvision>=0.15

# Optional -- only used by src/main.py's interactive 3D visualisation.
# plotly>=5.0
